export function aritmeticos(){
    let a = 10
    let b = 5

    let so1 = ("soma: ", a + b)
    let so2 = ("subtração: ", a - b)
    let so3 = ("multiplicação: ", a * b)
    let so4 = ("divisão: ", a/b)

    return [so1, so2, so3, so4]
}